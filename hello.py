# Define a function to return the greeting message
def say_hello():
    # This function simply returns the string "Hello, World!"
    return "Hello, World!"

# This block checks if the script is being run directly (not imported)
if __name__ == "__main__":
    # Call the function and print its result to the console
    print(say_hello())
